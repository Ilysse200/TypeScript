// String, boolean

let student:string;

student = 'Mugisha';

//The difference between Let and const is that later on let can change values but const doesn't

let studentAge: number = 9;

//Custom datatypes
let isActive: boolean = false;
